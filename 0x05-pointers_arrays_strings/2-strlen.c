#include "main.h"
#include <string.h>

/**
 * _strlen - prints the lenght of a string
 *
 * @s: is a variable that accept a string
 * Return: returns the value of lenght
 */

int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
